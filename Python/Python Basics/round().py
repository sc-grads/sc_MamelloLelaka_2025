x = 3.14159

print(round(x, 0))   # 3.0   (zero decimal places → nearest integer, but returns float)
print(round(x, 1))   # 3.1   (one decimal place)
print(round(x, 2))   # 3.14  (two decimal places)
print(round(x, 3))   # 3.142 (three decimal places)